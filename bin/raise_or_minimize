#!/bin/bash
active_window=$(xprop -root _NET_ACTIVE_WINDOW|cut -d ' ' -f 5|sed -e 's/../0&/2')
foo=${1:3}
echo $1
echo $foo
if [[ ${active_window} == *${foo}* ]]; then
  xdotool windowminimize "$1"
  echo "minim $1"
else
  wmctrl -ia $1
  echo "focus $1"
fi
